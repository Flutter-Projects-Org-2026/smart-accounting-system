name: flutter_application_1
description: نظام محاسبي للخدمات
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^4.7.0
  firebase_auth: ^6.4.0
  cloud_firestore: ^6.3.0
  google_sign_in: ^6.2.1
  http: ^1.2.2
  

  flutter_local_notifications: ^17.2.3
  timezone: ^0.9.4

  # إدارة الحالة
  provider: ^6.1.2

  # الرسوم البيانية
  fl_chart: ^0.71.0

  # تصدير PDF
  pdf: ^3.11.1
  printing: ^5.13.2
  path_provider: ^2.1.4

  # مكتبات مساعدة
  intl: ^0.20.2
  uuid: ^4.5.1
  shared_preferences: ^2.3.2
  google_fonts: ^6.2.1
  iconsax: ^0.0.8
  firebase_vertexai: ^2.2.0
  google_generative_ai: ^0.4.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

 

flutter:
  uses-material-design: true

  